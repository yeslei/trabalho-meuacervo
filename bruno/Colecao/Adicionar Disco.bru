meta {
  name: Adicionar Disco
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/colecao/adicionar
  body: json
  auth: none
}

headers {
  Accept: application/json
  Content-Type: application/json
}

body:json {
  {
    "id_disco": {{idDisco}}
  }
}

tests {
  test("adicionar colecao usa POST", function () {
    expect([200, 400, 401, 500]).to.include(res.status);
  });
}
